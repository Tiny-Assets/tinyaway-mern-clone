import styled from 'styled-components'; 

export default function FilterPill({name, func}) {
    return(
        <PillField />
    )
}

const PillField = styled.div`
    background-color: var(--corpYellow); 
    padding: 10px 20px; 
`